import { PokemonBgColorPipe } from './pokemon-bg-color.pipe';

describe('PokemonBgColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PokemonBgColorPipe();
    expect(pipe).toBeTruthy();
  });
});
